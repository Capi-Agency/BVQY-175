import { CommonFields } from '@/src/types/pageBuilder';

type Props = {
  data: CommonFields;
};

const NotFoundBlock = (props: Props) => {
  return <div>NotFoundBlock</div>;
};

export default NotFoundBlock;
